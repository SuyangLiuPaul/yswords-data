name: Refresh daily news

# Cron fires every 4 hours. Each run pulls RSS, hits the deep-match
# cache for stories already seen, and only commits if data/daily_news.json
# actually changes (the `git diff --cached --quiet` guard below).
#
# Why every 4 hours and not hourly (changed 2026-08-25):
#   • A deep-match that 429s is persisted as translationState=fallback
#     and RETRIED by every later run. At hourly, ~100 poisoned entries
#     cost ~200 calls an hour, so the daily quota died within an hour
#     or two of each midnight-PDT reset and never recovered. The
#     cadence was converting one bad run into a permanent one.
#   • Source RSS feeds (Guardian, BBC, SBS, DW) update on a 1–3 hour
#     rhythm anyway, so 24 polls/day mostly re-fetched the same stories
#     at full AI cost.
#   • 6 runs/day fits a ~130-story edition across the model chain
#     comfortably, and a run takes ~37 min, so there is no chance of
#     one still finishing as the next queues.
#
# Times land at 10:00 / 14:00 / 18:00 / 22:00 Sydney (plus 02:00 and
# 06:00), and the 08:00 UTC fire is an hour after the free tier's
# midnight-PDT reset — the cheapest slot in the day to do cold work.
#
# If coverage still falls short, the next levers in order of value are:
# several keys in GEMINI_API_KEYS (the round-robin in refresh-news.mjs
# already supports it), then a paid key. Do NOT go back to hourly.
#
# Previous setup before hourly was four Sydney hours (06/11/16/19)
# gated by scripts/schedule-window.mjs (file still present, unused).

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */4 * * *'

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest

    # Promote NETLIFY_AUTH_TOKEN to job-level env so step `if:` blocks
    # can branch on its presence. Neither `secrets.X` nor a step-level
    # `env.X` is readable from `if:`; only job-level / workflow-level
    # env vars are.
    env:
      NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Refresh news data
        env:
          # The refresh script accepts the key under any of these
          # variable names (preferring GEMINI_API_KEYS > GEMINI_API_KEY
          # > OPENAI_API_KEY). Wiring all three so renaming the secret
          # in GitHub Actions doesn't break AI enrichment.
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_API_KEYS: ${{ secrets.GEMINI_API_KEYS }}
          # 2026-08-25: was pinned to gemini-2.5-flash on the belief it
          # gives 1500 RPD. It does not — measured ~20 requests/day on
          # an untouched daily budget, which is why deep-match coverage
          # never got past a fifth of the edition. Leaving this unset
          # lets refresh-news.mjs pick its own default (flash-lite) and
          # walk OPENAI_MODEL_CHAIN on 429, instead of this line
          # silently overriding both. Set the OPENAI_MODEL repo secret
          # to pin a model (e.g. gemini-2.5-pro on a paid key).
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}
          NEWS_MIN_ITEMS_PER_SECTION: 10
          NEWS_MAX_ITEMS_PER_SECTION: 18
        run: npm run refresh

      - name: Validate refreshed data
        run: npm run validate

      - name: Regenerate manifest
        run: npm run manifest

      - name: Commit + push
        id: commit
        env:
          # 2026-05-24: piped through explicitly so the recovery path
          # below can re-embed the token into the remote URL after
          # `git reset --hard`. actions/checkout's extraheader auth
          # works for the FIRST push, but on long-running jobs (~6+ min)
          # combined with a `reset --hard` that follows a rejected
          # push, the second push sometimes fails with
          # "could not read Username for 'https://github.com'" —
          # the credential cache state gets dropped. Embedding the
          # token in the remote URL is the standard fix.
          GH_PUSH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/daily_news.json data/_manifest.json
          if git diff --cached --quiet; then
            echo "No data changes; skipping commit."
            echo "did_commit=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git commit -m "chore: refresh daily news"
          # Race-proof push.  A human or another concurrent cron run
          # can push between this run's checkout and our push, which
          # rejects with non-fast-forward.  A plain rebase fails when
          # both sides modified data/daily_news.json (different RSS
          # snapshots) — they're auto-generated, so trying to merge
          # the JSON line-by-line is meaningless.
          #
          # Strategy: stash our generated files, hard-reset to remote,
          # restore our files on top, recommit, push.  This is always
          # equivalent to "use this run's JSON, drop whatever the
          # racing cron produced" — which is what we want because
          # this run is by definition the freshest.
          if ! git push 2>&1; then
            echo "Push rejected — stashing & replaying on fresh remote."
            cp data/daily_news.json /tmp/daily_news.json.fresh
            cp data/_manifest.json /tmp/_manifest.json.fresh
            git fetch origin main
            git reset --hard origin/main
            cp /tmp/daily_news.json.fresh data/daily_news.json
            cp /tmp/_manifest.json.fresh data/_manifest.json
            git add data/daily_news.json data/_manifest.json
            if git diff --cached --quiet; then
              echo "Remote already had this exact content; nothing to push."
              echo "did_commit=false" >> "$GITHUB_OUTPUT"
              exit 0
            fi
            git commit -m "chore: refresh daily news"
            # 2026-05-24: explicit token in URL for the recovery
            # push. Bypasses the dropped-credential-cache class of
            # failure that surfaced in run 26331964618 (Refresh
            # daily news on 0d42b51, 11:53 UTC).
            git remote set-url origin \
              "https://x-access-token:${GH_PUSH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
            git push origin HEAD:main
          fi
          echo "did_commit=true" >> "$GITHUB_OUTPUT"

      # Two ways to deploy after a commit:
      #
      #   A. Direct CLI deploy (preferred — bypasses Netlify's git
      #      clone, which has been broken with "Host key verification
      #      failed" errors). Requires NETLIFY_AUTH_TOKEN secret.
      #      Deploys the local checkout straight to the CDN, so the
      #      JSON the bot just committed is on yswords-data.netlify.app
      #      within ~10 seconds.
      #
      #   B. Build-hook POST (fallback — only fires when the auth
      #      token isn't configured). Requires Netlify's git
      #      integration to be healthy, which it currently is not.
      #      Kept around so users who prefer hook-based deploys can
      #      still wire those in.
      #
      # Two consumers rebuild from the same JSON:
      #   1. yswords-data.netlify.app  — this repo's static deploy.
      #   2. newsbible.netlify.app     — Astro UI; build script pulls
      #      daily_news.json from yswords-data via sync-data.mjs then
      #      runs astro build.
      # NETLIFY_AUTH_TOKEN is promoted to job-level env (see top of
      # this job), which is the only env scope readable from step
      # `if:` expressions. `secrets.X` and step-level `env.X` are
      # both invisible to the conditional evaluator.
      - name: Deploy yswords-data via Netlify CLI
        if: steps.commit.outputs.did_commit == 'true' && env.NETLIFY_AUTH_TOKEN != ''
        run: |
          npx --yes netlify-cli@latest deploy \
            --prod \
            --dir=. \
            --site="${NETLIFY_SITE_ID_YSWORDS_DATA}" \
            --auth="${NETLIFY_AUTH_TOKEN}" \
            --message="cron refresh @ $(date -u +%FT%TZ)"
        env:
          NETLIFY_SITE_ID_YSWORDS_DATA: e1252e5a-a37e-4ba4-94ab-046ee9e6da9b

      - name: Trigger Netlify build hooks (legacy fallback)
        if: steps.commit.outputs.did_commit == 'true' && env.NETLIFY_AUTH_TOKEN == ''
        run: |
          if [ -n "$NETLIFY_BUILD_HOOK" ]; then
            curl -fsS -X POST "$NETLIFY_BUILD_HOOK" -d '{}' || true
            echo "Triggered yswords-data build via hook."
          else
            echo "Neither NETLIFY_AUTH_TOKEN nor NETLIFY_BUILD_HOOK set; new commit will sit unpublished."
          fi
        env:
          NETLIFY_BUILD_HOOK: ${{ secrets.NETLIFY_BUILD_HOOK }}

      # newsbible.netlify.app rebuild — build hooks still work for it
      # because the broken-link issue is on the yswords-data site,
      # not the DailyNews site. Fires regardless of which yswords-data
      # path was used above.
      - name: Trigger DailyNews (newsbible) rebuild
        if: steps.commit.outputs.did_commit == 'true'
        run: |
          if [ -n "$DAILYNEWS_BUILD_HOOK" ]; then
            curl -fsS -X POST "$DAILYNEWS_BUILD_HOOK" -d '{}' || true
            echo "Triggered DailyNews build via hook."
          else
            echo "DAILYNEWS_BUILD_HOOK not set; newsbible.netlify.app will not auto-rebuild."
          fi
        env:
          DAILYNEWS_BUILD_HOOK: ${{ secrets.DAILYNEWS_BUILD_HOOK }}
